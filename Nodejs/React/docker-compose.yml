version: '3.8'

services:
  frontend:
    build:
      context: ./docker/frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app
    ports:
      - 3000:3000
    environment: 
      - NODE_ENV=development
    tty: true
    command: /bin/sh -c 'yarn start'
